name: ci

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      tags: 
         description: 'Run Tags'


jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          push: true
          file: Dockerfile
          tags: morzlee/l4d2:latest
      -
        name: ssh key to home deploy new docker image
        uses: appleboy/ssh-action@master
        with:
          host: "${{ secrets.HOST1 }}"
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            bash ~/autoupdate.sh &
      -
        name: ssh key to guangzhou server deploy new docker image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST2 }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            bash ~/autoupdate.sh &
      -
        name: ssh key to guangzhou server2 deploy new docker image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST3 }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            bash ~/autoupdate.sh &
      -
        name: ssh key to shanghai server deploy new docker image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST4 }}
          username: ${{ secrets.USERNAME2 }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo su -
            bash ~/autoupdate.sh &
        name: ssh key to shanghai server2 deploy new docker image
      -
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST5 }}
          username: ${{ secrets.USERNAME2 }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo su -
            bash ~/autoupdate.sh &